package committee.nova.forge.item;

import net.minecraft.item.Item;

public class GuideEnvironmental extends Item {
    public GuideEnvironmental(Settings settings) {
        super(settings);
    }
}
